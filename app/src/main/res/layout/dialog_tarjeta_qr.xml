<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#1E293B"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="NEFROSCAN - PASAPORTE CLÍNICO"
        android:textColor="#38BDF8"
        android:textSize="14sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="4dp"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Identificación Digital Offline"
        android:textColor="#94A3B8"
        android:textSize="12sp"
        android:gravity="center"
        android:layout_marginBottom="20dp"/>

    <!-- Imagen del QR conectada con el ID ivQrCodeDialog -->
    <ImageView
        android:id="@+id/ivQrCodeDialog"
        android:layout_width="180dp"
        android:layout_height="180dp"
        android:background="#FFFFFF"
        android:padding="8dp"
        android:contentDescription="Código QR del Paciente"
        android:layout_marginBottom="16dp"/>

    <!-- Texto de Información conectado con el ID tvInfoQrDialog -->
    <TextView
        android:id="@+id/tvInfoQrDialog"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Cargando información..."
        android:textColor="#FFFFFF"
        android:textSize="15sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="16dp"/>

</LinearLayout>